# Instance runtime data (created by docker compose)
instances/*/data/
instances/*/.env

# Runtime-generated configs (from templates)
instances/*/openclaw.json
template/openclaw.json

# Fleet registry (local state)
fleet.json

# Template env (users copy to .env)
!template/.env.example
!instances/*/.env.example

# OS
.DS_Store
instances/
archives/
